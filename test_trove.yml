---
version: 0.1.0
commands:
  - name: new_command
    namespace: default
    tags:
      - home
      - change
      - directory
    command: cd ~
    description: Change current working directory to home directory of the user
  - name: new_command2
    namespace: meshcapade
    tags:
      - home
      - change
      - directory
    command: cd ~
    description: Change current working directory to home directory of the user
