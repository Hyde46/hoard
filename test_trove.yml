---
version: 0.1.0
commands:
  - name: go_home
    namespace: default
    tags:
      - home
      - change
      - directory
    command: cd ~
    description: Change current working directory to home directory of the user
  - name: build_image
    namespace: hyde46
    tags:
      - docker
      - build
    command: docker build
    description: builds an image
  - name: pwding
    namespace: hyde46
    tags:
      - "no"
      - tags
    command: pwd
    description: lol as if
  - name: new_command
    namespace: meshcapade
    tags:
      - home
      - change
      - directory
    command: cd ~
    description: Change current working directory to home directory of the user
  - name: new_command2
    namespace: default
    tags:
      - home
      - change
      - directory
    command: cd /home/
    description: Change current working directory to home directory of the user
